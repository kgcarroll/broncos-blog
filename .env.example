# Sanity (public reads + Studio)
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2025-01-01

# Site URL (Presentation / stega; production: https://your-domain.vercel.app)
NEXT_PUBLIC_SITE_URL=http://localhost:3333

# Optional: viewer token for draft mode + live preview (sanity.io/manage ? API ? Tokens)
# SANITY_API_READ_TOKEN=
